# Migration `20191223112857`

This migration has been generated by Guillermo at 12/23/2019, 11:28:57 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."AuthorizationCode" (
  "client_id" TEXT    ,
  "client_secret" TEXT    ,
  "code" TEXT NOT NULL DEFAULT ''  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "redirect_uri" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "quaint"."Scope" (
  "authorizationCode" TEXT    REFERENCES "AuthorizationCode"(id) ON DELETE SET NULL,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "description" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Client" (
  "client_id" TEXT    ,
  "client_secret" TEXT    ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "description" TEXT NOT NULL DEFAULT ''  ,
  "homepage_url" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "redirect_uri" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Client" ("client_id","client_secret","description","homepage_url","id","name") SELECT "client_id","client_secret","description","homepage_url","id","name" from "Client"

DROP TABLE "quaint"."Client";

ALTER TABLE "quaint"."new_Client" RENAME TO "Client";

CREATE UNIQUE INDEX "quaint"."Client.client_id" ON "Client"("client_id")

CREATE UNIQUE INDEX "quaint"."Client.client_secret" ON "Client"("client_secret")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_User" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "password" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "username" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_User" ("email","id","name") SELECT "email","id","name" from "User"

DROP TABLE "quaint"."User";

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "quaint"."User.username" ON "User"("username")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

CREATE UNIQUE INDEX "quaint"."AuthorizationCode.client_id" ON "AuthorizationCode"("client_id")

CREATE UNIQUE INDEX "quaint"."AuthorizationCode.client_secret" ON "AuthorizationCode"("client_secret")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20191223011750..20191223112857
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model Client {
   id            String   @default(cuid()) @id
@@ -13,16 +13,40 @@
   client_secret String?  @unique
   name          String
   description   String
   homepage_url  String
-  callback_url  String
+  redirect_uri  String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 }
+model AuthorizationCode {
+  id            String   @default(cuid()) @id
+  client_id     String?  @unique
+  client_secret String?  @unique
+  redirect_uri  String
+  code          String
+  scope         Scope[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model Scope {
+  id            String   @default(cuid()) @id
+  name          String
+  description   String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
 model User {
-  id    String  @default(cuid()) @id
-  email String  @unique
-  name  String?
-  posts Post[]
+  id       String  @default(cuid()) @id
+  email    String  @unique
+  username String  @unique
+  password String
+  name     String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 }
 model Post {
   id        String   @default(cuid()) @id
```


